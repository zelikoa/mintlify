---
title: GPT-4
description: 'Feel the power of GPT-4'
---

export const Button = ({ children, color }) => (
  <button
    style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.5rem 1rem',
      cursor: 'default', // To indicate it's not clickable
      border: 'none',
      outline: 'none',
    }}
  >
    {children}
  </button>
);

// Example usage:
<Button color="#808080">Click me (Does nothing)</Button>

I can write **Markdown** alongside my _JSX_!

<form id="gptForm">
  <label for="prompt">Enter your prompt here:</label>
  <input
    type="text"
    id="prompt"
    name="prompt"
    placeholder="Enter your prompt here"
    class="dark"
  />
  <br />
  <input
    type="button"
    value="Submit"
    class="dark"
    onclick="submitForm()"
  />
</form>
