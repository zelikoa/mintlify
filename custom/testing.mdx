---
title: Testing
description: 'This page is for testing purposes'
---

I can write **Markdown** alongside my _JSX_!!!!!

export const TextInputBox = ({ value, onChange }) => {
  return (
    <input
      type="text"
      value={value}
      onChange={(e) => onChange(e.target.value)}
      style={{
        padding: '0.5rem',
        fontSize: '1rem',
        border: '1px solid #ccc',
        borderRadius: '4px',
      }}
    />
  );
};

<TextInputBox value={textInputValue} onChange={handleInputChange} />

