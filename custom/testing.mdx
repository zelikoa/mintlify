---
title: Testing
description: 'This page is for testing purposes'
---

I can write **Markdown** alongside my _JSX_!!!!!

export const handleButtonClick = () => {
    console.log(`Docusaurus green clicked 1`);
    console.log(`Docusaurus green clicked 2`);
};

export const Button = ({ children, color, onClick }) => (
  <button
    style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.5rem 1rem',
      cursor: 'pointer',
      border: 'none',
      outline: 'none',
    }}
    onClick={onClick}
  >
    {children}
  </button>
);

<input
  id="promptInputBox"
  type="text"
  placeholder="Enter your prompt here"
  className="dark"
/>

<Button color="#25c2a0" onClick={handleButtonClick}>
  Submit
</Button>

