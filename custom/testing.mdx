---
title: Testing
description: 'This page is for testing purposes'
---

I can write **Markdown** alongside my _JSX_!!!!!

export const Button = ({ children, color, onClick }) => (
  <button
    style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.5rem 1rem',
      cursor: 'pointer',
      border: 'none',
      outline: 'none',
    }}
    onClick={onClick}
  >
    {children}
  </button>
);

<Button color="#25c2a0" onClick={() => console.log('Docusaurus green clicked')}>
  Click me (Docusaurus green)
</Button>

export const TextInput = ({ value, onChange, placeholder, highlightColor }) => (
    <div>
      <label style={{ color: '#333', marginBottom: '0.5rem', display: 'block' }}>
        Enter text:
      </label>
      <input
        type="text"
        value={value}
        onChange={onChange}
        placeholder={placeholder}
        style={{
          padding: '0.5rem',
          borderRadius: '4px',
          border: `2px solid ${highlightColor}`,
          outline: 'none',
          width: '300px',
        }}
      />
    </div>
);

<TextInput
    value={inputValue}
    onChange={handleInputChange}
    placeholder="Type here"
    highlightColor="#25c2a0"
/>
